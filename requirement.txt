# Core data processing and analysis
pandas>=1.3.0
scikit-learn>=1.0.0

# Text processing and NLP
nltk>=3.6.2
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.2  # Optional but improves fuzzywuzzy performance

# Google Generative AI API
google-generativeai>=0.3.0

# Concurrency
futures>=3.0.5  # For Python 2.x compatibility if needed
# If using Python 3.x, concurrent.futures is in the standard library

# Optional dependencies for improved performance
numpy>=1.20.0
tqdm>=4.62.0  # For progress bars (optional)
